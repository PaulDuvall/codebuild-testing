version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - node --version
      - npm install newman --global
  build:
    commands:
      - 'newman run -r html,json,junit,cli collection.json'
artifacts:
  files:
    - newman/*
